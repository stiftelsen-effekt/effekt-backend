module.exports = {
    db_connection_string: "" //Insert mongoDB connection string
}

//Remove everything down from here when config is setup
console.error(('\x1b[' + '31' + 'm') + "Error: " + ('\x1b[' + '37' + 'm') + "Change config.js in root before running") 
process.exit()